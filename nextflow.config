manifest {
    mainScript = 'main.nf'
}

docker { enabled = true }

// default parameters

params {
    cores = '4'
    help = false
    chunks = '0'

    // folder structure
    output = 'results'
    blastdir = '01-Blast'

    // inputs
    fasta = ''
    db = ''


}
